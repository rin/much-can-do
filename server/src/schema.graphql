type Query {
  skills: Skill!
}

type Skill {
  id: ID!
  title: String!
  strength: Int!
}

type Mutation {
  createSkill(title: String!, strength: Int!): Skill!
  upadateSkill(id: ID!, title: String, strength: Int): Skill!
  deleteSkill(id: ID!): Skill!
}